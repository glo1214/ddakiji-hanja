# 배포 체크리스트 (코드 도구에서 할 일)

Cowork에서 내용 작업(개념 시드, 비열 콘텐츠 등)은 끝났고, 실제 배포만 코드 도구(Claude Code)에서 하면 됨.
아래 지시문을 코드 도구에 그대로 붙여넣으면 됨.

---

## 1) 코드 도구에 붙여넣을 지시문

> 이 폴더는 딱이지 한자앱(github: glo1214/ddakiji-hanja, Vercel 프로젝트: ddakiji-hanja)이야.
> 앱이 Vite에서 Next.js로 이관됐는데 아직 커밋이 하나도 안 됐어. 다음을 해줘:
> 1. `.git/index.lock` 파일이 있으면 지워줘.
> 2. 변경 전부 커밋해줘 (.env.local·node_modules는 .gitignore로 제외됨).
> 3. Vercel 프로젝트 설정에서 Framework Preset을 Next.js로 바꿔줘 (지금 Vite로 돼 있을 것).
> 4. Firebase 환경변수를 `NEXT_PUBLIC_` 이름으로 Vercel에 넣어줘 (아래 목록).
> 5. main 브랜치로 push해서 Vercel 자동 배포를 돌려줘. 빌드 성공하면 라이브 URL 알려줘.

---

## 2) 챙길 3가지 (이것만 되면 배포됨)

1. **잠금 파일 삭제** — `.git/index.lock` (Cowork가 못 지운 잔여 파일. 코드 도구는 지울 수 있음)
2. **프레임워크 = Next.js** — Vercel Settings → General → Framework Preset (Vite → Next.js)
3. **환경변수** — 아래 값을 Vercel Settings → Environment Variables에 추가

---

## 3) 필요한 환경변수

Firebase 콘솔 > 프로젝트 설정 > 내 앱 > SDK 설정에서 값 복사 (옛 `VITE_` 이름은 안 먹힘, 이 이름으로):

```
NEXT_PUBLIC_FIREBASE_API_KEY
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN
NEXT_PUBLIC_FIREBASE_PROJECT_ID
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID
NEXT_PUBLIC_FIREBASE_APP_ID
```

- `OPENAI_API_KEY` — 선택. 없으면 설명하기 자동채점만 규칙 기반으로 폴백되고 앱은 정상 작동. 나중에 넣어도 됨.
- `OPENAI_MODEL` — 선택 (기본값 gpt-5.4-mini).

---

## 4) 배포 후 확인

- Vercel 빌드가 성공(초록)인지.
- 라이브에서 `/learn/sci_37` 열어 비열이 5단계로 도는지.
- 빌드 실패해도 기존 라이브는 유지됨(성공해야 교체) → 부담 없이 재시도 가능.

---

## 참고: 지금까지 Cowork에서 한 작업 (모두 폴더에 저장됨, 커밋만 하면 됨)

- 개념 메타데이터 시드 병합: `개념_메타데이터_시드_중등_v1.json` / `.csv` (과학 50 · 사회 34 = 84개)
- 검토용 목록·시드 문서: `중1_사회과학_개념어_목록_검토용_v1.md`, `개념_메타데이터_시드_중1사회_v1.md`, `개념_메타데이터_시드_중1과학_v1.md`
- 비열 콘텐츠 모듈: `lib/learn/content/sci_37.ts` + 레지스트리 `lib/learn/content/index.ts`, 페이지 연결 `app/learn/[conceptId]/page.tsx`
- 두 앱 비교: `두앱_비교_한자게임vs5단계_v1.md`

### 아직 코드에 미반영 (미리보기만 함 — 다음에 반영할 것)
- 한자 짝 맞추기 UI, 4MAT 순서 재배치(느낌·그림 앞으로), 질문형 비계(무엇이/어떻게/왜), 비주얼 싱킹(ConceptExample에 비열 그림 + DrawingPad를 5단계에 연결)
