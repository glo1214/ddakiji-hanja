# 학생 데이터 모델 & 설명하기 입력 설계 v1

> 효율 노트: #1(정량)과 #2(설명하기 태깅)는 결국 **학생 한 명의 같은 기록**에 쌓인다.
> 그래서 둘을 따로 만들지 않고 **하나의 학생 데이터 모델**로 잡는다. 어려운 #2를 자세히, #1을 같은 틀에 끼운다.

---

## 큰 그림 — 학생 기록 3층

```
학생
 ├─ 정량 층 (#1)        시간·단어 수·단계 진행
 ├─ 정성 층 (#2)        설명하기 답안 + 오류 태그
 └─ 누적·처방 층        태그 빈도 → 처방 우선순위
```

정량은 "얼마나·어디까지", 정성은 "어떻게 틀렸나", 누적은 "그래서 뭘 시킬까". 세 층이 같은 학생 ID 아래 묶인다.

---

## #2 설명하기 입력 설계 (핵심)

### 1) 캡처 방식 — 2단(자유 → 비계)

- **1단 자유 서술:** 아무 도움 없이 먼저 쓰게 한다. 여기서 범주화 약점이 자연스럽게 드러난다(= 측정 목적). 멈칫·'것'을 그대로 기록.
- **2단 비계(막힐 때만 호출):** 한 단어로 닫기, `재료 → 과정 → 결과` 슬롯, 한자 뜻 힌트. 항상 주면 못 재니까 **재시도/요청 시에만** 연다.
- 이게 "선택 단계" 철학과 맞물린다 — 잘하면 1단에서 끝, 막히면 비계를 부르고 *그 호출 자체가 진단 신호*가 된다.

### 2) 캡처할 원시 신호 (raw signals)

| 신호 | 잡는 오류 |
|------|-----------|
| 응답 지연·수정 횟수(멈칫) | ② 용어 인출 실패 |
| '것/거/뭐냐면' 빈도 | ② |
| 핵심 요소 체크리스트 충족 여부 | ④ 핵심 누락 |
| 상위어 vs 정확 용어 | ① 범주 오류 |
| 순서·연결어(그래서·때문에) 유무 | ③ 구조·순서 오류 |
| 짝 개념 속성 혼입 | ⑤ 혼동·오개념 |
| 구어체 vs 정의문 형식 | ⑥ 표현 부정확 |

> 자유 텍스트 판정은 LLM이 하되, 아래 '정답 메타데이터'를 같이 주면 신뢰도가 크게 오른다.

### 3) 개념별 '정답 메타데이터' (콘텐츠팀이 작성)

태깅이 되려면 개념마다 작은 메타가 필요하다. 광합성 예:

```yaml
concept: 광합성
정확주어: 식물
흔한상위어: [생물, 동물]      # ① 감지
닫는명사: 과정                # ② 감지 (정의를 닫는 말)
핵심요소: [빛, 물, 이산화탄소, 양분(포도당)]   # ④ 감지
짝개념: 호흡                  # ⑤ 감지
한자: 光(빛) 合(합할) 成(이룰)  # 2단 비계용
```

이 메타 한 벌이 곧 채점 기준 + 비계 재료 + 처방 근거 셋을 동시에 한다. 개념을 추가할 때 이 틀만 채우면 자동 태깅이 따라온다.

### 4) 출력 — 답안 1건 → 태그 집합

```
설명하기답안 = {
  원문,
  신호값들: { 멈칫수, '것'빈도, 충족요소[], ... },
  태그[]:    [① , ②],          # 복수 허용
  처방단계[]: [개념정리]         # 태그→되돌릴 선택 단계
}
```

정답률처럼 단일 점수가 아니라 **태그 집합**으로 남긴다. 한 답에 여러 태그가 붙는다.

---

## #1 정량 층 설계 (같은 틀에 끼움)

- **세션 단위:** 시작·종료 시각 → 공부 시간. 순공 시간은 무활동 타임아웃으로 보정.
- **단어 단위:** 학습 단어 수 / 복습 단어 수, 단어별 도달 단계(느낌·개념·따라·크게·설명).
- **단계 진행:** 필수(설명) 완료 여부 + 선택 단계 사용 패턴.
- **숨은 진단:** *어떤 선택 단계를 자주 부르는지* 자체가 신호 — 특정 유형 단계를 반복 호출하면 그쪽이 약하다는 뜻. (#2의 비계 호출 기록과 같은 결)

---

## 통합 스키마 (한 장 요약)

```
학생
 ├─ 정량
 │   ├─ 총 공부시간, 세션수
 │   ├─ 학습단어수 / 복습단어수
 │   └─ 단계별 도달 분포 + 선택단계 호출 패턴
 ├─ 단어별 기록[]
 │   ├─ 객관식: 정답률
 │   └─ 설명하기답안[] → 태그집합
 └─ 누적 진단
     ├─ 오류태그 빈도 (①~⑥)
     └─ 처방 우선순위 (예: ①②가 잦음 → 범주화 훈련 먼저)
```

핵심: 선생님이 광합성 한 사례로 직관한 "이 학생 범주화가 약하다"를, 앱이 **태그 누적**으로 데이터화해 확인해준다.

---

## 검증 — 광합성 답안 손으로 통과시키기

입력: "생물이 영양분을 얻는 **것**" (멈칫)

| 단계 | 결과 |
|------|------|
| 신호 추출 | 상위어 '생물' 등장(정확주어=식물) → ① / '것' + 멈칫 → ② / 핵심요소 빛·물 등 미등장 → ④ 후보 |
| 태그 | **[①, ②]** (+④는 답이 짧아서 보류 가능) |
| 처방 단계 | 개념 정리 |
| 누적 | ①② 빈도 +1 → 반복되면 "범주화 훈련 우선" |

→ 의도대로 **①+②**가 나온다. 선생님의 직관과 일치.

**주의(측정 오염):** 2단 비계를 너무 일찍 주면 1단의 진짜 약점이 안 잡힌다. 비계는 *멈칫·요청 감지 후*에만 열 것.

---

*v1 — 개념 메타데이터 양식이 콘텐츠 제작의 기준점이 됨. 사례 쌓이면 신호 임계값 보정 예정.*
