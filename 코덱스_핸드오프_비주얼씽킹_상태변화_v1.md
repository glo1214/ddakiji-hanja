# 코덱스 핸드오프 — 비주얼씽킹 선아트 (물질의 상태 변화 10개) v1

> **스타일:** 네가 이미 만든 `public/concept-images/concept-*.png`의 선아트 그대로(흑백 얇은 선, 무색·무텍스트, 흰 배경).
> **이번 핵심 원칙:** 입자 구조만 따로 그리지 말 것(교과서와 똑같아짐). **익숙한 사물 안에 입자를 비쳐 보이게** 겹쳐서, "이 사물 속이 사실은 이렇게 배열돼 있다"가 한 그림에 담기게.
> **저장 규칙:** `public/concept-images/vt-<로마자>.svg` (기존 `concept-<로마자>.png`와 짝. 아래 파일명 그대로 저장하면 앱이 자동 연결됨.)
> **상태변화 공통:** 변화 개념은 **변화 전 사물+입자 → (열 화살표) → 변화 후 사물+입자**. 열을 얻으면 화살표 안으로, 잃으면 밖으로.

| 개념 | 파일명 | 그릴 것 (사물 + 입자) | 오개념 — 이렇게 그리면 안 됨 |
|---|---|---|---|
| 고체 | `vt-goche.svg` | 얼음 큐브 안에 입자가 **규칙적·촘촘한 격자**로, 제자리에서 살짝 진동(작은 떨림 표시) | 입자가 완전히 멈춘 것처럼 X — 제자리 진동은 함. 모양·부피 고정 |
| 액체 | `vt-aekche.svg` | 물이 담긴 컵 안에 입자가 **가깝지만 자유롭게 미끄러지는** 느슨한 배열, 흐름 곡선 | 입자 사이가 텅 비어 멀리 떨어진 것 X(그건 기체). 부피는 유지 |
| 기체 | `vt-giche.svg` | 투명 용기 안에 입자가 **멀리 흩어져 사방으로** 날아다님(방향 화살표) | 위로만 올라가는 것 X — 사방으로 퍼져 공간을 가득 채움 |
| 융해 | `vt-yunghae.svg` | 얼음(규칙 격자) → 열 화살표 **안으로** → 물(느슨) | 녹는 동안 온도 계속 오르는 것 X(녹는점 일정). 물질 사라짐 X |
| 응고 | `vt-eunggo.svg` | 물(느슨) → 열 화살표 **밖으로**(방출) → 얼음(규칙 격자) | '차가움'만 강조 X — 핵심은 배열 고정+열 방출. 응결과 혼동 X |
| 기화 | `vt-gihwa.svg` | 주전자 물(느슨) → 열 안으로 → 위로 흩어지는 입자(기체), 김 | 하얀 '김'을 기체로 X — 김은 물방울(액체), 진짜 수증기는 투명 |
| 액화 | `vt-aekhwa.svg` | 흩어진 입자(기체) → 열 밖으로 → 차가운 컵 겉면에 모여 물방울 | 컵 안 물이 새어나온 것 X — 공기 중 수증기가 식어 맺힘 |
| 승화 | `vt-seunghwa.svg` | 드라이아이스(규칙 격자) → **곧바로** 흩어지는 입자(기체), 액체 단계 없음(중간 액체에 X 표시) | 녹아 액체 거쳐 기체 되는 것 X — 액체 없이 직접 |
| 응결 | `vt-eunggyeol.svg` | 공기 중 흩어진 수증기 입자 → 모여서 풀잎 위 이슬방울(액체) | 이슬을 하늘에서 내린 물로 X — 공기 중 수증기가 맺힘. 응고와 혼동 X |
| 증발 | `vt-jeungbal.svg` | 웅덩이/빨래에서 **표면의 입자 몇 개만** 톡톡 튀어 나감(끓음처럼 전체 X) | 부글부글 끓음으로 X — 표면에서, 낮은 온도에서도, 조용히 |

## 입자 표기 약속 (일관성용)
- 입자 = 작은 검정 원(채움). 배열만 다르게: **고체=격자로 촘촘 / 액체=느슨·접촉 / 기체=멀리 흩어짐**.
- 열 = 화살표. **얻음(융해·기화)=사물 쪽으로 들어가는 화살표 / 잃음(응고·액화)=사물에서 나가는 화살표**.
- 사물 윤곽은 선, 필요하면 아주 옅은 채움(불투명도 5% 정도)까지만. 색·글자·배경 없음.

## 앱 연결 (내가 처리)
코덱스가 위 파일명으로 저장하면, 각 개념 콘텐츠 모듈의 `visual.image`를 `/concept-images/vt-<로마자>.svg`로 연결하고 `kind`는 `대칭비교`(상태변화 짝) 또는 `묶음맵`(고체·액체·기체)으로 지정. 비열은 이미 `vt-biyeol.svg`로 연결됨.

*v1 — 상태변화 10개. 확정되면 힘·빛·지권·생물, 사회 개념도 같은 표 형식으로 이어서 넘김.*
