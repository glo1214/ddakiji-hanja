# 두 앱 비교 — 딱이지 한자게임 vs 5단계 개념앱 (v1)

> 결정을 위한 정리. 둘 다 **같은 Next.js 저장소** 안에 공존 중.
> - 딱이지 한자게임 = `app/HanjaGame.tsx` (배포: ddakiji-hanja.vercel.app)
> - 5단계 개념앱 = `app/learn/[conceptId]` + `lib/learn/*` + 개념 시드 JSON

---

## 한눈에

| 항목 | 딱이지 한자게임 | 5단계 개념앱 |
|---|---|---|
| **핵심 철학** | 한자를 쪼개 개념어 뜻을 여는 **게임** | 개념을 여러 각도로 익히고 **설명하기로 이해 측정·진단** |
| **학습 흐름** | 지식 트리(선수개념 잠금) → 개념별 6칸 | 느낌→개념→따라해보기→크게보기→**설명하기(필수)** |
| **UX 성격** | 수집·해금형 게임, 그리기 패드 | 유형별 진입점·스킵, 진단·처방 |
| **개념 수/과목** | 과학 위주 약 30개 (한자 있는 말만) | 과학 50 + 사회 34 = **84개** (고유어·사회 포함) |
| **콘텐츠 위치** | 코드에 하드코딩(`CONCEPTS` 객체) | 메타(JSON 시드) + 콘텐츠 모듈(`content/[id].ts`) **분리** |
| **오류 태그** | 인지오류 5종 | 서술오류 6종 |
| **채점** | 객관식 정답 + 설명 키워드 매칭 | 객관식(태그 보기) + 주관식 **교차분석 2×2** + 자동태깅 |

---

## 데이터 모델 차이

**한자게임** (개념 1개 = 코드 블록)
```
term, hanja[{sound, meaning}], easyMeaning, example,
quiz[{q, options, correct, hint, wrongTags}],
explanation{keywords, minRequired, modelAnswer},
prerequisites[], isBonus
```

**5단계앱** (메타 + 콘텐츠 2층)
```
[메타/시드]  correct_subject, over_general, closing_noun,
            core_elements, confusable, hanja[[자,뜻]], target_errors
[콘텐츠]     feel, concept, quiz, bigPicture, explain
```

→ 한자게임은 **한 파일에 다 있고**, 5단계앱은 **채점 기준(메타)과 학생 화면(콘텐츠)을 나눠** 둠. 5단계 쪽이 데이터 재사용·자동태깅엔 유리하고, 한자게임 쪽이 개념 하나 만들기엔 단순.

## 오류 태그 매핑 (1:1은 아님)

| 한자게임 | ≈ | 5단계앱 |
|---|---|---|
| literal_meaning (글자 그대로) | ≈ | ⑤혼동 / (한자 비계로 처방) |
| overgeneralization (과일반화) | ≈ | ①범주 |
| location_confusion (장소 혼동) | ≈ | ④핵심 (조건 누락) |
| cause_effect_reversal (인과 역전) | ≈ | ③구조 |
| structure_order_confusion (순서 혼동) | ≈ | ③구조 |
| — | | ②용어 (닫는명사→'것/거') |

→ 대응은 되지만 5단계 쪽이 **서술 채점**에 맞춰 더 잘게 나뉨. 합치려면 태그 변환표가 하나 필요.

## 겹치는 개념 (양쪽에 다 있음)

변이 · 원핵생물 · 생물다양성 · 광합성 · 고체 · 액체 · 기체 · 비열 …

→ 이 개념들은 한자게임에 이미 **한자분해·쉬운뜻·예시·퀴즈**가 작성돼 있음. 5단계앱의 **②개념 단계 재료로 거의 그대로** 쓸 수 있음(가장 큰 재활용 지점).

---

## 합칠 수 있는 지점

1. **한자게임 콘텐츠 → 5단계 ②개념 단계 재료.** `hanja`·`easyMeaning`·`example`이 5단계 `concept` 블록과 직결.
2. **한자게임 퀴즈 → 5단계 ③따라해보기 객관식.** `wrongTags`만 6종 체계로 변환.
3. **지식 트리(선수개념 잠금)** — 5단계앱엔 없는 개념 순서·해금 아이디어. 도입하면 학습 경로가 생김.
4. **공유 컴포넌트** — `DrawingPad`·`ConceptExample`는 이미 두 앱이 같이 씀(저장소 공용).

## 각 방식의 장단

**A. 한자게임을 5단계 '개념' 단계로 흡수 (통합)**
- 장점: 앱 하나로 단순화, 이미 쓴 한자 콘텐츠 재활용, 채점·리포트 일원화.
- 단점: 게임의 트리·해금 재미가 5단계 틀에 눌릴 수 있음. 태그 변환·마이그레이션 작업 필요.

**B. 두 앱 별도 유지**
- 장점: 한자게임의 게임성 보존, 각자 목적에 최적화. 당장 손댈 것 없음.
- 단점: 개념·콘텐츠 이중 관리, 데이터 갈라짐, 학생 입장에서 앱 두 개.

**C. 절충 — 데이터는 하나, 표현은 둘**
- 한 개념 시드에서 **한자게임 모드**와 **5단계 모드** 둘 다 렌더.
- 장점: 콘텐츠 단일 출처 + 게임성 유지.
- 단점: 초기 설계 비용 가장 큼(공용 스키마 설계).

---

## 추천

겹치는 과학 개념이 많고, 한자게임의 한자 콘텐츠가 5단계 **②개념 단계에 거의 그대로 맞으므로**, 실용적 1순위는 **A(흡수) 또는 C(데이터 통합)**. 당장은 **A로 시범** — 겹치는 개념 1개(예: 비열 또는 변이)를 한자게임 콘텐츠를 재료 삼아 5단계 콘텐츠 모듈로 옮겨보고, 잘 맞으면 트리·해금을 C로 확장하는 순서를 권함.

*v1 — 코드(`HanjaGame.tsx`, `lib/learn/*`, 시드 JSON) 기준. 실제 통합 범위는 시범 1개 해보고 확정 권장.*
